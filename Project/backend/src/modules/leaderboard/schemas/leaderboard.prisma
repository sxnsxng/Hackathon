model LeaderboardEntry {
  id         Int      @id @default(autoincrement())
  userId     String
  username   String
  score      Int
  role       String
  dayReached Int
  ending     String
  createdAt  DateTime @default(now())
}

